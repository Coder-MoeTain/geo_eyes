# Security Hardening

- Enforce JWT access + refresh rotation.
- Revoke refresh tokens on logout.
- Enable login brute-force lockout.
- Restrict CORS to known origins.
- Use HttpOnly/SameSite cookies for browser auth.
- Audit all privileged actions (model ops, detections, exports, admin).
